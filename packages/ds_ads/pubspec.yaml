name: ds_ads
description: A new Flutter package for AdMob integration.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.1.0
  injectable: ^2.3.2
  get_it: ^7.6.4
  shimmer: ^3.0.0
  lottie: ^3.1.3
  get: ^4.6.6
  flutter_screenutil: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  injectable_generator: ^2.4.1

flutter:
