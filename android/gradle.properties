org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Disabling parallel execution avoids Windows file locking contention during AGP CXX log generation
org.gradle.parallel=false
# Force single ABI during IDE sync to prevent multi-threaded log file locks
android.injected.build.abi=arm64-v8a
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
